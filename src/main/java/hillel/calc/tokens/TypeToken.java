package hillel.calc.tokens;

/**
 * there are some type of token that helping to parse string expression into list of tokens
 */
public enum TypeToken {
    OPERAND,
    OPERATION,
    FUNCTION,
    OPEN_BRACKET,
    CLOSE_BRACKET,
    DEVIDER
}
